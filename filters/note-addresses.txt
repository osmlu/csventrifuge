What we filter in dicacolo are roads with no addresses (lieux dits), so we don't filter addresses. We want the addresses for a lieu dit. Eventually, we could process those specially to be converted to addr:place instead of addr:streetname...